{
  "name": "etherpulse-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker secure proxy for EtherPulse AI — handles Gemini API calls server-side",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put GEMINI_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
